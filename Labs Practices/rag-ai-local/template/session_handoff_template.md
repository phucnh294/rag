---
title: <Human title — e.g. "Handoff: Fixing Test Runner Issues">
date: <YYYY-MM-DD>
type: session-handoff
area: <feature-slug>
status: <in-progress|implementation-complete|superseded>
session_id: <session id>
next_action: <one concrete sentence + the exact command to resume>
supersedes: <previous handoff filename, or omit>
tags: [tag1, tag2]
keywords: []
related: []
---

## TL;DR
- **What:** one line — what this session worked on.
- **Why:** why it mattered / the problem being solved.
- **Where:** the files/components touched.
- **Impact:** the outcome so far — a real metric, not "good progress".

## Current Status
<Real metric — e.g. "56/67 tests passing (83.6%)", not "good progress".>

## COMPLETED
- <item> — build/test state confirmed by: `<command>` → `<result>`

## NOT DONE / STILL OPEN
- <item> — `path/to/file.py:123` — <what's missing, specifically>

## NEXT ACTION
<The single first thing to do next, plus the exact command to resume.>

## CONTEXT THE NEXT SESSION CANNOT DERIVE FROM CODE
- **Decision:** <what was decided> — **Reason:** <why> — **Rejected alternative:** <what else was considered and why it lost>
- **Trap:** <the thing that looks right but isn't> — **What actually happens:** <the real behavior>
- **Ground truth:** <a fact verified from logs/DB> — **Verified where:** <exact source>
